@model HomeViewModel

@await Html.PartialAsync("Carousel");

<h2>Prefered products of the week</h2>
<h4>Clients preferences - just for you</h4>

<div class="row">
    @foreach (var product in Model.PrefferedProducts)
    {
        @Html.Partial("ProductSummary",product);
    }
</div>